
import 'package:flutter/material.dart';

class ConstrainsLearning extends StatelessWidget {
  const ConstrainsLearning({Key? key}) : super(key: key);


  @override
  Widget build(BuildContext context) {

    Color red = Colors.red;
    Color green = Colors.green;
    return Container(
      color: red,
      width: 10,
      height: 10,
    );
  }
}
